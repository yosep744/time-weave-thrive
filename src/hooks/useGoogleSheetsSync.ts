// Auto-sync disabled - Google Sheets sync requires OAuth2 authentication
// Users can manually sync via the GoogleSheetsSync component
export const useGoogleSheetsSync = () => {
  // No auto-sync to prevent authentication errors
};
